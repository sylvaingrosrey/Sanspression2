{% extends 'base.html.twig' %}

{% block title %}Horaires{% endblock %}

{% block body %}
<div class="container ml-5 mr-5">
<div class="row ">

<div class="col-12">
    <h1>Horaires</h1>

    <table class="table ">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ horaires.id }}</td>
            </tr>
            <tr>
                <th>Jours</th>
                <td>{{ horaires.jours }}</td>
            </tr>
            <tr>
                <th>Ouverture</th>
                <td>{{ horaires.Ouverture }}</td>
            </tr>
            <tr>
                <th>Fermeture</th>
                <td>{{ horaires.Fermeture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('horaires_index') }}" class="btn btn-outline-primary">back to list</a>

    <a href="{{ path('horaires_edit', {'id': horaires.id}) }}" class="btn btn-outline-secondary ml-5">edit</a>

    {{ include('horaires/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
{% endblock %}
