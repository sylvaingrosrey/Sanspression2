{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<div class="container ml-5 mr-5">
<div class="row ">

<div class="col-12">
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>SrcImg</th>
                <td>{{ category.srcImg }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}" class="btn btn-outline-primary">back to list</a>

    <a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-outline-secondary ml-5">edit</a>

    {{ include('category/_delete_form.html.twig') }}
     </div>
    </div> 
    </div>
    
{% endblock %}
