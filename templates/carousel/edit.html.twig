{% extends 'base.html.twig' %}

{% block title %}Edit Carousel{% endblock %}

{% block body %}
<div class="container ml-5 mr-5">
<div class="row ">

<div class="col-12">
    <h1>Edit Carousel</h1>

    {{ include('carousel/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('carousel_index') }}" class="btn btn-outline-primary mb-3">back to list</a>

    {{ include('carousel/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
{% endblock %}
