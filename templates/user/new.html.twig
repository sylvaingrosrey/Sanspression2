{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
<div class="container ml-5 mr-5">
<div class="row ">

<div class="col-12">
    <h1>Create new User</h1>

    {{ include('user/_form.html.twig') }}

    <a href="{{ path('user_index', {'id': user.id}) }}" class="btn btn-outline-primary mb-4"><=back to list</a>
     </div>
    </div>
    </div>
{% endblock %}