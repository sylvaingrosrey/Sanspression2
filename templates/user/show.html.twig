{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="container ml-5 mr-5">
<div class="row ">

<div class="col-12">
    <h1>User</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ user.name }}</td>
        </tr>
        <tr>
            <th>PhoneNumber</th>
            <td>{{ user.phoneNumber }}</td>
        </tr>
        <tr>
            <th>Adresse</th>
            <td>{{ user.adresse }}</td>
        </tr>
        <tr>
            <th>Cp</th>
            <td>{{ user.cp }}</td>
        </tr>
     
        </tbody>
    </table>

    <a href="{{ path('home') }}" class="btn btn-outline-primary">back to home</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-outline-secondary ml-5">edit</a>
<a href="{{ path("reloadpassword") }}" class="btn btn-outline-info ml-5 mb-3">Change password</a>
    {{ include('user/_delete_form.html.twig') }}
     </div>
    </div>
    </div>
{% endblock %}